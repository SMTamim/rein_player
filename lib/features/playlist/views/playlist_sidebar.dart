import 'package:flutter/material.dart';

class RpPlaylistSideBar extends StatelessWidget {
  const RpPlaylistSideBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Video side bar");
  }
}
