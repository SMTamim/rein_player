class PlaylistItem {
  PlaylistItem({
    required this.name,
    required this.location,
    required this.id,
    this.currentVideo = "",
  });

  String name, location, id, currentVideo;

// list of files
// list of folders
}
